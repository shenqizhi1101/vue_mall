// import { debounce } from 'conponents/common/debounce';

export const itemListMixin = {
  mounted () {
    //  mixin 这个是用来保存页面重复代码的js文件
    // console.log(this.$route.path)
    // console.log(1212)
  }
}
